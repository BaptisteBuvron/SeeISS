<header>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('home') }}">SeeISS</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mb-2 mb-lg-0 me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="#">Les Passages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('live') }}">Track ISS</a>
                    </li>
                </ul>
                <ul class="navbar-nav mb-2 mb-lg-0 ml-auto">
                    <li class="nav-item dropdown ms-auto">
                        {% if app.request.locale == "fr" %}
                            <a class="nav-link dropdown-toggle" href="{{ path('language_selector', {'lang' : 'fr'}) }}" id="dropdown09" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span class="flag-icon flag-icon-fr"> </span> Français</a>
                        {% else %}
                            <a class="nav-link dropdown-toggle" href="{{ path('language_selector', {'lang' : 'en'}) }}" id="dropdown09" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span class="flag-icon flag-icon-us"> </span> English</a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="dropdown09">
                            {% if app.request.locale == 'fr' %}
                            <a class="dropdown-item" href="{{ path('language_selector', {'lang' : 'en'}) }}"><span class="flag-icon flag-icon-us"> </span>  English</a>
                            {% else %}
                            <a class="dropdown-item" href="{{ path('language_selector', {'lang' : 'fr'}) }}"><span class="flag-icon flag-icon-fr"> </span>  Français</a>
                            {% endif %}
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="logo">
        <h1>See ISS</h1>
        <p>{% trans %}by {% endtrans %} Baptiste BUVRON</p>
    </div>
</header>